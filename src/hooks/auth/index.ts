/**
 * Authentication Hooks
 * 
 * Custom hooks for managing authentication state and user data
 */

export { useAuth } from './useAuth';
export { useCurrentUser } from './useCurrentUser';
export { useUserRole } from './useUserRole';
