/**
 * Barrel export for all Firebase type definitions
 * 
 * This file re-exports all types from individual type definition files,
 * allowing consumers to import from a single location.
 * 
 * @example
 * import { User, Student, Attendance } from '@/firebase/types';
 */

// User types
export * from './user.types';

// Student types
export * from './student.types';

// Attendance types
export * from './attendance.types';

// Financial types
export * from './financial.types';

// Event types
export * from './event.types';
